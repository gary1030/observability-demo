apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: opentelemetry-demo-server-metrics
  labels:
    team: techfresh
    monitoring: prometheus
spec:
  namespaceSelector:
    matchNames:
      - techfresh
  selector:
    matchLabels:
      app: opentelemetry-demo-server
  endpoints:
    - interval: 15s
      targetPort: metrics
      path: /metrics
