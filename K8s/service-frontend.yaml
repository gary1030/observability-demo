apiVersion: v1
kind: Service
metadata:
  name: opentelemetry-demo-tellme-app-service
  labels:
    app: opentelemetry-demo-tellme-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: frontend
      protocol: TCP
  selector:
    app: opentelemetry-demo-tellme-app
